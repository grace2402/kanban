services:
  kanban-board-app:
    build: .
    container_name: kanban-board-app
    ports:
      - "5001:5001"
    env_file:
      - .env
    environment:
      - TZ=Asia/Taipei
    restart: unless-stopped

networks:
  default:
    external:
      name: teaasia-main_default
